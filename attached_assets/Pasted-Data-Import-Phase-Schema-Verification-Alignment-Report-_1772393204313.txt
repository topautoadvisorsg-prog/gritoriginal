Data Import Phase — Schema Verification & Alignment Report Required

We are entering the data importing phase.

Before we begin importing fighters and fight history, I need a complete schema alignment report to ensure every field matches exactly between:

Database schema

API layer

Import payload structure

Frontend expectations

We cannot afford mismatched field names, missing columns, or silent type coercion issues.

1️⃣ Fighter Import — Full Schema Report Required

Provide a structured report for the fighters table including:

For each field:

Exact field name

Data type

Nullable or required

Default value (if any)

Validation rules

Relationships (foreign keys)

Where it is used (API, UI, scoring logic, tags, etc.)

Example format:

Field Name:
Data Type:
Required:
Default:
Used In:
Notes:

Confirm:

All expected fighter attributes exist

Tag relationships are correctly linked

Fighter reference in news_articles is correctly structured

No deprecated fields remain

No mismatched naming between DB and API

2️⃣ Fight History Import — Full Schema Report Required

Same structure required for:

fights

fight_results

event linkage

fighter references

scoring dependencies

Clarify:

How fights detect fighter identity (ID vs name)

How event_id is linked

How winner/draw/NC is stored

How partial scoring logic reads results

We must ensure:

No reliance on fighter name matching

Only ID-based relationships

Referential integrity enforced

3️⃣ Synchronization Verification (Critical)

While preparing the schema report, confirm:

API request body fields match DB column names

No field renaming between layers

Validation schema matches DB types

Import endpoint expects the same structure as DB

If any mapping layer exists (DTO, transformer, etc.), document it clearly.

We need zero ambiguity.

4️⃣ Import Detection Logic Clarification

Explain:

How duplicate fighters are detected (ID? name? combination?)

How duplicate fights are prevented

Whether unique constraints are in place

What happens if a required field is missing

5️⃣ Deliverables

Before we import any data, provide:

Fighter schema report

Fight history schema report

Confirmation that both are fully synchronized

Confirmation that no field drift exists