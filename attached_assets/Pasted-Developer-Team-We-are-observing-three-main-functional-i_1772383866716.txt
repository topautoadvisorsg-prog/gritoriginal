Developer Team,

We are observing three main functional issues during Closed Beta testing.

Please audit the following subsystems.

━━━━━━━━━━━━━━━━━━━━

1. Admin Panel Visibility Issue (Priority)
   ━━━━━━━━━━━━━━━━━━━━

Symptom:

* After login, admin tabs are not visible.
* User dashboard loads but admin routes are hidden.

Probable Cause:

* AuthContext hydration timing.
* Backend role field not properly injected during OAuth callback.
* Sidebar permission filtering before session state resolves.

Required Fix Checkpoints:

• Verify /api/auth/user endpoint returns:
{
id,
email,
role,
tier,
permissions
}

Specifically confirm:

* role === 'admin' is written during login callback when email matches ADMIN_EMAIL.

Frontend Rendering Guard:

Layout shell should wait for authLoading === false before rendering navigation modules.

━━━━━━━━━━━━━━━━━━━━
2. Premium Export Feature (Tier Gate)
━━━━━━━━━━━━━━━━━━━━

Export functionality must be protected server-side using:

requireTier('premium')

Do not rely on frontend visibility alone.

Users should be able to:

* See export button (optional UX)
* Only premium users can execute export API calls.

━━━━━━━━━━━━━━━━━━━━
3. Advertisement Close Button Behavior
━━━━━━━━━━━━━━━━━━━━

Ads must be dismissible.

Implement state-controlled ad visibility:

* User can click X to hide ads.
* Temporary dismissal is acceptable.
* Consider storing dismissal timestamp if persistence is desired.

━━━━━━━━━━━━━━━━━━━━
4. Recommended Audit Checks

Please verify:

A. OAuth Login Flow
Login → Provider → Callback → Session → Role Injection → UI Render

B. Sidebar Permission Logic
Ensure module visibility depends on hydrated user permissions.

C. Router Rendering Sequence
Main layout should not render protected navigation until authentication state is resolved.

━━━━━━━━━━━━━━━━━━━━
Testing Focus After Fix:

Please validate:

* Admin login visibility
* Event creation
* Fighter profile import
* Fight finalization scoring
* Prestige key unlock
* Leaderboard snapshot

━━━━━━━━━━━━━━━━━━━━
Current Assessment

Architecture integrity is good.

Remaining risk appears to be frontend auth state hydration timing.

Thank you.

— GRIT Development Review
