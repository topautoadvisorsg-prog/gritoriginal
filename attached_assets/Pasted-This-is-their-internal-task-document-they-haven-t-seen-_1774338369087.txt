This is their internal task document — they haven't seen your question yet, but this gives us a lot of the answers we needed anyway. Let me read their sync schema file references they mentioned to get the exact field names before we do anything.

Good, this is their internal task plan — it answers a lot but also reveals something critical. Here's the full breakdown of what we now know and what it means for us.

What their document just told us
The single biggest mismatch — and it's a fatal one:

Our pipeline manager is pushing to these URLs:

/api/pipeline/fighters
/api/pipeline/fights
/api/pipeline/events
/api/pipeline/news
/api/pipeline/odds
None of those exist on their end. They have a single webhook endpoint:

POST /api/webhooks/data-engine/webhook
Every single push we make right now would hit a 404. We need to change all five push methods to use that one URL.

What else we confirmed from their doc
Our assumption	Reality	Status
Auth header x-data-engine-api-key	✅ Correct	Good
sourceType field in wrapper	✅ They use it to route payloads	Good
performance as nested JSONB object	✅ Stored as JSONB	Good — but field names must match exactly
imageUrl for fighters	✅ Correct, stored directly	Good
bodyImageUrl	❌ We never send this	Missing — they have a second image field
gym for fighters	❌ We send team, not gym	Field name mismatch
Fight history location field	❌ We don't send it	They're adding support for it
Fight history fightType, eventName, eventDate	❌ We don't send these	They're adding support for it
News fighterReference / eventReference	❌ We don't send these	We never link news to a fighter UUID
Odds format fighter1Odds/fighter2Odds	✅ Matches	Good
What we still don't know yet
Before we finalize our code changes, we still need their actual sync-schemas.ts file because:

We don't know the exact field names their Zod schema expects for the performance block — if they expect koWins but we send ko_wins, it silently fails Zod validation
We don't know what bodyImageUrl represents — is it a full-body shot? Where would we get it?
We don't know the exact shape of the location object for fights — { city, country } or { locationCity, locationCountry } or something else?
We don't know what the full wrapper looks like on their end — do they want dataType: "mma_data" or is that ignored?
My recommendation — send them this follow-up message now
Send this alongside (or before) the original message I drafted:

"Before you send me your question, I got the document you shared. I can see the webhook is at /api/webhooks/data-engine/webhook — that's what we'll point everything at. To finalize our end, I need one thing: can you share the contents of shared/sync-schemas.ts? That's the Zod validation file listed in your doc. I need the exact field names for the fighter, fight, event, news, and odds schemas so our payloads pass validation. Also — what is bodyImageUrl? Is that a full-body photo, and do you want us supplying it?"

The plan once we have their answer
There are 4 clear tasks to fix the integration:

Task 1 — Fix all push URLs (can do now, no info needed)
All 5 push methods in pipeline_manager.py point to the wrong endpoints. Change them all to POST /api/webhooks/data-engine/webhook.

Task 2 — Fix the fighter payload (need their schema to confirm field names)
Rename team → gym, add bodyImageUrl, confirm performance object key names are exactly what their Zod schema expects.

Task 3 — Fix the fight history payload (can start, wait for location shape)
Add eventName, eventDate, fightType, and location object to each bout push. Also update our database schema to store these fields since Agent 3 doesn't collect them yet.

Task 4 — Fix news payload (need fighter UUID linking)
Add fighterReference — the GRIT UUID of any fighter mentioned in the article. This requires our news agent to link articles to fighters when storing them.

Want me to go ahead and propose these as formal tasks so you can assign them? Task 1 can start immediately while you wait for their schema file.