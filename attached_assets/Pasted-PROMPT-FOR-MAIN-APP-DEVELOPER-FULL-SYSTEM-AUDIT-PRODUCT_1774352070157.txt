PROMPT FOR MAIN APP DEVELOPER:

FULL SYSTEM AUDIT — PRODUCTION READINESS REPORT
Stop everything you are working on. This is the most important thing you will do before we go live. I need a complete honest audit of the entire platform. Read every word before you start.

STEP 1 — READ THE README COMPLETELY
Before you open a single file read the entire README from top to bottom. Every system. Every rule. Every feature. Every flow. That is your source of truth. Everything you find in this audit gets measured against what the README says should be there.

STEP 2 — FULL CODEBASE AUDIT
Go through every module, every route, every service, every component. Compare what is in the code against what the README says should be there. Flag everything using this system:
🔴 Red — wrong, broken, or does not match README. Must be fixed before launch.
🟡 Yellow — incomplete, or in README but not fully built, or needs discussion.
🟢 Green — confirmed correct, working, matches README.
🔵 Blue — suggestion or improvement that would make the product better.
For every flag tell me the file, the issue, and your recommendation.

STEP 3 — SYSTEM BY SYSTEM CONFIRMATION
Go through every system and give me a completion percentage and honest status:
Flag system — yellow counts toward ranking, red excludes, moneyline only
Ranking formula — net units only, real odds math, lockedOdds on every pick
Leaderboard — per event, monthly, yearly all generating correctly
Participation formula — fixed card size minimums, real time banner, 70% formula completely gone
Raffle — month 1 exclusion, $0.50 contribution, draw logic, cron trigger
Tag system — database driven, admin manages without code, real backend filtering
Pick board — view toggle, fighter highlights, share image generation
Dashboard — every widget on real data, zero mock data
Winning streak — event level only, 2 or more consecutive positive events
Real betting tracker — private, unit size configurable, P&L correct
Community chat — admin toggle, free vs paid tiers, founder badges in chat
Slip system — profile upload, admin queue, 7 day expiry, chat posting, cooldown
Slip wall — featured slips on landing page, admin controls
Data engine pipeline — intake endpoints, staging, admin approval, Zod validation
Cron jobs — monthly snapshot, yearly snapshot, slip cleanup, progression runs
Landing page — correct copy, atmospheric background, fighter placeholders, slip wall section, pricing two cards correct
For each one give me:

Completion percentage
What is working
What is not working or incomplete
What is needed to get it to 100%


STEP 4 — DATA ENGINE INTEGRATION READINESS
Specifically confirm the following are ready to receive live data from the data engine:

POST /api/webhooks/data-engine/webhook — live, authenticated, Zod validated
Fighter profile intake — all fields from sync bible ready, gym nullable, no crash on missing optional fields
Fight history intake — location, fightType, eventName, eventDate all optional, no crash on missing fields
Event intake — lockTime field present and wired into pick submission gate
News and intelligence intake — layer field correct, tags defaulting to empty, fighterReference and eventReference mapping correctly
Odds intake — JSONB format ready, lockedOdds being set at pick submission
Admin pipeline dashboard — approve, reject, apply all working for every source type
Duplicate detection — sourceId match routes to update, name fuzzy match flags as potential duplicate

Give me a yes or no on each one and flag anything that is not fully confirmed.

STEP 5 — UI AND UX HONEST ASSESSMENT
Go through every user facing page with fresh eyes. You are not looking for bugs. You are looking for anything that feels wrong, unfinished, or below the standard of a product you would pay for. Specifically:

Dead moments where user actions get no feedback
Empty states that are blank instead of engaging
Loading states that are missing
Mobile pages that are broken or cramped
Micro interactions that are missing — hover states, press animations, number count ups
Sections that feel flat or template-like instead of premium
Typography or spacing that looks off
Anything that would make a new user confused or lost

Be honest. Tell me what a first time user would think when they land on this product today.

STEP 6 — PERFORMANCE AND SECURITY
Flag anything that concerns you:

Unprotected API endpoints
Queries that will be slow at scale — missing indexes, unoptimized joins
User data that could be exposed to other users
Errors that crash pages instead of being handled gracefully
Hardcoded values that should be in environment variables
Unnecessary API calls on every render


STEP 7 — PRODUCTION READINESS SCORE
Give me an honest overall score out of 100 for production readiness. Break it down like this:

Core functionality: X out of 100
Data engine integration: X out of 100
UI and UX quality: X out of 100
Performance and security: X out of 100
Landing page: X out of 100
Overall: X out of 100

Then tell me — what are the top 5 things that if fixed right now would have the biggest impact on getting this to a product you could confidently show to a UFC executive.