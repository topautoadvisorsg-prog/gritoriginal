m — Admin System Audit & Simulation Prompt
Objective

Validate all administrative control paths, including:

Event management lifecycle

Fight result finalization

Badge / Prestige system authority

Leaderboard snapshot integrity

Moderation controls

Data audit logging

Maintain:

Clean architecture

Typed code structure

No dead code branches

No deletion without replacement implementation

Documentation synchronization with README

1. Admin Identity Enforcement

Admin access must be controlled by:

user.role === 'admin'

Backend must be the source of truth.

Frontend role state is not authoritative.

Test:

Direct API call to admin routes from non-admin session → must be blocked.

2. Event Administration Testing

Simulate full event creation pipeline.

Admin must be able to:

Create Events

Generate fantasy test events with:

12–30 fighters per card

Dummy metadata allowed

Populate all schema fields.

Event Status Lifecycle Control

Test transitions:

Upcoming → Live → Completed → Closed → Archived

Rules:

Picks must lock when event becomes Live or time ≥ event start.

Once finalized, picks cannot be modified.

3. Fight Result Finalization Authority

Only admin can execute:

POST /api/fights/:fightId/result

When executed:

System must atomically process:

Pick evaluation

Score distribution

Ledger history insertion

Prestige key logic

Badge audit logging

Leaderboard snapshot trigger

Concurrent Execution Test

Attempt:

Double finalize request simulation.

Expected behavior:

Second request must fail gracefully or be ignored.

No duplicate scoring.

4. Prestige & Badge System Audit

Test milestone logic:

Clean Sweep Detection

Award Prestige Key ONLY IF:

User achieves 100% prediction accuracy.

Database constraint:

UNIQUE(userId, eventId)

Must prevent duplicate key awards.

Ultra Badge Unlock

When user collects 5 keys:

Automatically unlock Ultra Badge.

Log action in badge audit table.

Audit logging required.

5. Moderation Control Validation

Admin must be able to:

Block user

Mute user

Resolve reports

Review flagged chat messages

Test abuse resistance:

Attempt posting chat message after block → must fail.

6. Leaderboard Integrity

Leaderboard must be derived from:

Total Accuracy %
Recent Performance (3 events momentum)
Participation Rate

Test snapshot system:

Manual snapshot trigger must not overwrite historical snapshots.

7. AI Module Governance

Admin override priority:

Admin access > Premium tier > Medium tier > Free tier

Test AI prediction module gating.

8. Routing Security Audit

Verify:

/admin/* routes are wrapped in:

RequireAdmin Guard

If non-admin visits → redirect to root.

9. Readme Synchronization Rule

Developer must follow this rule:

✅ If system behavior changes → update README
✅ If new module is added → document architecture
✅ Do not delete documented system logic without replacement implementation
✅ Maintain clean explanatory documentation

10. Code Hygiene Requirements

Enforce:

Typed interfaces

No any casts unless absolutely necessary

Remove dead branches

Avoid redundant tier checks

Keep modular separation

11. Test Simulation Requirement

Developer must simulate:

Create multiple events

Create fighters

Finalize fights

Award badges

Test moderation

Test leaderboard snapshot

Test routing

Do not create permanent production data.

Delete test entities after verification.

12. Final Admin Stability Target

System must demonstrate:

✅ Transaction atomicity
✅ Role isolation
✅ Badge uniqueness
✅ Routing correctness
✅ Audit traceability
✅ Scoring determinism
✅ Documentation consistency

Closing Instruction

Continue refining backend safety, integrity enforcement, and documentation alignment if any edge case is discovered.
The administrative control layer must remain deterministic and tamper-resistant.